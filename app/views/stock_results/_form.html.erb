<%= form_for @stock_result, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :stock, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :stock, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :result_date, :class => 'control-label' %>
    <div class="controls">
      <%= f.date_select :result_date, :class => 'date_select' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                stock_results_path, :class => 'btn' %>
  </div>
<% end %>
